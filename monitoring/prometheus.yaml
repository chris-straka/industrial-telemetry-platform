global:
    scrape_interval: 30s

scrape_configs:
    - job_name: "otel-collector"
      static_configs:
          # otel-collector is docker service name
          - targets: ["otel-collector:8889"]

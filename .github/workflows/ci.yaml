name: Build and Test

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore .NET dependencies
        run: dotnet restore IndustrialPlatform.slnx

      - name: Build .NET solution
        run: dotnet build IndustrialPlatform.slnx --configuration Release --no-restore --warnaserror

      - name: Verify .NET formatting
        run: dotnet format IndustrialPlatform.slnx --no-restore --verify-no-changes

      - name: Restore local .NET tools
        run: dotnet tool restore

      - name: Verify EF migration snapshot
        run: dotnet ef migrations has-pending-model-changes --project src/Industrial.Diagnostics.Worker --configuration Release --no-build --no-color

      - name: Run regression tests
        run: dotnet test IndustrialPlatform.slnx --configuration Release --no-build --collect:"XPlat Code Coverage"

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: src/Industrial.Web.Dashboard/package-lock.json

      - name: Install dashboard dependencies
        working-directory: src/Industrial.Web.Dashboard
        run: npm ci

      - name: Lint dashboard
        working-directory: src/Industrial.Web.Dashboard
        run: npm run lint

      - name: Build dashboard
        working-directory: src/Industrial.Web.Dashboard
        run: npm run build

      - name: Validate Compose model
        run: docker compose config --quiet
        env:
          GEMINI_API_KEY: ci-config-validation-only

      - uses: azure/setup-helm@v4
        with:
          version: v3.16.4

      - name: Build and lint Helm chart
        run: |
          helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
          helm repo add grafana https://grafana.github.io/helm-charts
          helm dependency build chart
          helm lint chart

      - name: Render default and optional Helm resources
        run: |
          helm template industrial-platform chart --namespace industrial-platform --output-dir "$RUNNER_TEMP/helm-default"
          helm template industrial-platform chart \
            --namespace industrial-platform \
            --output-dir "$RUNNER_TEMP/helm-optional" \
            --set externalSecret.enabled=true \
            --set externalSecret.remoteRefs.geminiApiKey.key=ci/gemini \
            --set externalSecret.remoteRefs.postgresConnectionString.key=ci/postgres-connection \
            --set externalSecret.remoteRefs.postgresPassword.key=ci/postgres-password \
            --set istio.enabled=true \
            --set transportSecurity.enabled=true \
            --set transportSecurity.ingestion.secretName=ci-ingestion-mtls \
            --set transportSecurity.edgeGateway.secretName=ci-edge-mtls
          helm lint chart \
            --set externalSecret.enabled=true \
            --set externalSecret.remoteRefs.geminiApiKey.key=ci/gemini \
            --set externalSecret.remoteRefs.postgresConnectionString.key=ci/postgres-connection \
            --set externalSecret.remoteRefs.postgresPassword.key=ci/postgres-password \
            --set istio.enabled=true \
            --set transportSecurity.enabled=true \
            --set transportSecurity.ingestion.secretName=ci-ingestion-mtls \
            --set transportSecurity.edgeGateway.secretName=ci-edge-mtls

  e2e:
    needs: verify
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4

      - name: Run isolated failure and recovery suite
        run: ./scripts/e2e.sh
        env:
          GEMINI_API_KEY: e2e-config-validation-only
